apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pv-claim
  labels:
    app: vprodb
spec:
  # Remove the 'default' name. 
  # This tells EKS: "Use whatever the default storage provider is."
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
